apiVersion: v1
kind: Service
metadata:
  labels:
    app: carbon-api
    project: carbon-aware-scheduler
    component: master
    environment: prod
  name: cas-carbon-api-prod
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: carbon-api
    project: carbon-aware-scheduler
    component: master
    environment: prod
  type: ClusterIP
